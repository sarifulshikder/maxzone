# Node / Next.js local build artifacts
node_modules
.next
out
build
*.tsbuildinfo

# Environment & secrets — must never bake into the image
.env
.env.*
!.env.example

# VCS & tooling
.git
.gitignore
Dockerfile
.dockerignore
*.log
.DS_Store
README.md